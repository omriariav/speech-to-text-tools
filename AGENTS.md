# AGENTS.md

<!-- ai-handoff:start -->
# Codex Handoff Context

This section is managed by ai-handoff. Keep durable project rules outside the managed markers.
Codex loads AGENTS.md automatically when it works in this project; use this managed section as handoff context before starting work.

Generated: 2026-06-01T15:13:02Z
Flow: Claude Code -> Codex
Source project: /Users/omri.a/Code/speech-to-text-tools
Suggested goal: Prepare /Users/omri.a/Code/speech-to-text-tools for Codex handoff from Claude Code context.

## Project Guidance
- CLAUDE.md was found and used as source guidance.
- CLAUDE.md headings: CLAUDE.md; Project Overview; Setup; Create and activate virtual environment; Install dependencies; FFmpeg required for video_converter.py and audio_splitter.py; Core Scripts; transcribe.py; Single file; Explicit output path

## Recent Claude Work
- Selected sessions: 10 of 10 discovered.
- look at recent error in log (2026-05-31T19:13:50.444815Z)
  Summary: look at recent error in log
- please avoid showing failed push notifications for gdoc files. it's ok that they're failing (2026-05-20T16:02:41.688790Z)
  Summary: please avoid showing failed push notifications for gdoc files. it's ok that they're failing
- check the logs - do we listen to /Users/omri.a/Library/CloudStorage/GoogleDrive-omri.a\@taboola.com/My\ Drive/Meet\ R... (2026-05-19T15:52:30.140441Z)
  Summary: check the logs - do we listen to /Users/omri.a/Library/CloudStorage/GoogleDrive-omri.a\@taboola.com/My\ Drive/Meet\ Recordings as well?
- <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT resp... (2026-05-12T19:16:49.936194Z)
  Summary: <local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consi...
- I just hit another OOM; what are our options besides dropping to medium? (2026-05-12T09:58:25.196742Z)
  Summary: I just hit another OOM; what are our options besides dropping to medium?
- I am thinking of using 
https://github.com/JuergenFleiss/atrain_core
and/or
https://github.com/SYSTRAN/faster-whisper... (2026-05-11T09:02:33.219866Z)
  Summary: I am thinking of using 
https://github.com/JuergenFleiss/atrain_core
and/or
https://github.com/SYSTRAN/faster-whisper
to expedite the time it takes us to tra...
- write me the python command that takes the following recordings and transctibe them + diarlizae them they're in hebre... (2026-01-26T13:36:02.871Z)
- Hebrew 1x1 Meeting Transcription with Diarization (2026-01-26T13:09:53.303Z)
  Summary: Hebrew 1x1 Meeting Transcription with Diarization
- speech-to-text-tools (2026-01-26T12:15:30.941Z)
  Summary: README & Automator updated, diarization added
- speech-to-text-tool (2026-01-07T22:13:30.552Z)
  Summary: Diarization testing: success, then failure

## Claude Tooling Used
- MCPs: none
- Skills: tweet, x:tweet, codex:rescue, rescue, +5 more
- Plugins: x, codex, copy

## Claude Setup Captured
- Hooks: 9 captured, 0 enabled automatically; review required before recreating hooks.
- Rules: CLAUDE.md guidance is represented in AGENTS.md; 120 Claude permission rule(s) captured.
- References: 0 recorded (0 project-local, 0 external); external references are not copied automatically.
- Statusline: 1 captured only; Codex does not use Claude statusline rendering yet.

## Codex Tooling Prepared
- Installed Codex-wide for future Codex sessions:
  - plugin:copy@omri-cc-stuff
  - plugin:x@omri-cc-stuff
- Open a new Codex session after Codex-wide plugin or skill installs.
- Full install details are in .codex/handoff/manifest.json.

## Commands Detected
- No package or pyproject commands detected; inspect README and source before running tests.

## Handoff Artifacts
- Detailed summary: .codex/handoff/summary.md
- Latest manifest: .codex/handoff/manifest.json

## Safety Notes
- Codex-wide MCP, plugin, and skill installs require explicit confirmation.
- Review Codex-wide install details in the manifest before running or changing them.
<!-- ai-handoff:end -->

## Durable Project Rules

_Keep notes here, outside the managed handoff block above (that block is regenerated and will overwrite edits)._

### Google Drive shortcut files (.gdoc, .gsheet, ...)
These are tiny JSON pointers to cloud-native docs, not media. A "Notes by
Gemini" `.gdoc` lands in the Meet Recordings folder sharing the real
recording's name stem.

- They are **rejected at enqueue time** in `auto_transcribe_meet.sh` (case
  in the extension check, before the enqueue lock). They must never enter
  the queue — otherwise the fuzzy name-containment dedupe swallows the real
  recording as a "duplicate" and it never gets transcribed.
- `auto_transcribe_worker.sh` also skips the FAILED toast for these as a
  defense-in-depth fallback for jobs queued before the enqueue filter.
- Rejected/suppressed extensions: `gdoc gsheet gslides gdraw gform gmap
  gsite gtable glink gjam gscript`. **Keep the two lists in sync.**

### Drive-API poller (`fetch_drive_recordings.sh`)
The Meet Recordings folder is now triggered by an hourly Drive-API poll, not a
Folder Action. It downloads recordings directly via the `gws` CLI (bypassing
the unreliable Google Drive File Stream materialization) and feeds them to the
normal enqueue path. The Downloads Folder Action stays for manual local drops.

- **`DRIVE_FOLDER_ID` is LOCAL-ONLY.** It lives in `.env` (gitignored) and must
  never appear in a committed file — not `.env.example`, not the scripts, not
  the docs, not the LaunchAgent plist. `.env.example` carries an empty
  placeholder; the plist reads the ID from `.env` at runtime.
- Idempotency is keyed on the stable Drive **file ID** via `DRIVE_LEDGER_DIR`
  (one marker file per ID). Delete a marker to force a re-fetch; pre-seed the
  ledger to ignore an existing backlog.
- Only `video/mp4` is fetched, so Gemini "Notes" gdocs are excluded structurally
  (no shortcut-file dedupe hazard on this path).
- Hourly via a local LaunchAgent based on
  `com.speech-to-text-tools.drivepoll.plist.example`; keep the installed plist
  local if it contains user-specific labels or absolute paths.
